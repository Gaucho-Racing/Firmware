cmake_minimum_required(VERSION 3.30)
project(GRLib)

include(CMakePrintHelpers)
include(CTest)
enable_testing()

enable_language(C ASM)

# build unit tests here
cmake_print_variables(TOOLCHAIN_PREFIX)
# add_executable, add_executable(...)
# link to test, add_test(NAME ... COMMAND $<TARGET_FILE:...>)
# repeat

# GR474RE
include(../ECU-25/cmake/gcc-arm-none-eabi.cmake)
add_library(
    ECULib
ECU-lib/Src/CANdler.c
ECU-lib/Src/driving.c
ECU-lib/Src/inverter.c
ECU-lib/Src/pinging.c
ECU-lib/Src/stateMachine.c
ECU-lib/Src/utils.c
)
target_include_directories(ECULib PUBLIC
    ECU-lib/Inc
)

# Whatever platform the Dash Panel uses
include(../DashPanel-25/cmake/gcc-arm-none-eabi.cmake)
cmake_print_variables(TARGET_FLAGS)

