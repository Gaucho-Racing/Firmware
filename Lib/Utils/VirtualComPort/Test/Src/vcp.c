#include "vcp.h"

#include <stdint.h>

#include "Unused.h"

void VCP_Send(const char *data, uint32_t length)
{
	UNUSED(data);
	UNUSED(length);
}

void Setup_VCP(VCP_Config *input_config)
{
	UNUSED(input_config);
}
